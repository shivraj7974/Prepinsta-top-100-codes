#include<iostream>
#include<algorithm>
using namespace std;
void sortarray(int arr[], int n)
{
    sort(arr,arr+n);
    for(int i=0; i<n ; i++)
        cout<<arr[i]<<" ";
    
}
int main(){
    int arr[]={22,4,55,78,8,90,0};
    int n=sizeof(arr)/sizeof(arr[0]);
    sortarray(arr,n);
}
